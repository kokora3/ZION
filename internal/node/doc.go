// Package node will orchestrate runtime startup, shutdown, and subsystem
// lifecycles. It may depend on lower-level packages; lower-level packages must
// not depend on node. No runtime is implemented in Phase 1.
package node
