# Phase 3 identity vector fixtures

This directory is reserved for deterministic, cross-language identity fixtures. Any seed material used in a fixture must be marked **TEST ONLY — PUBLIC FIXTURE — NOT SECRET — NEVER USE IN PRODUCTION**.
